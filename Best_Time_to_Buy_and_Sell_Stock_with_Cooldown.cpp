#include <vector>

using namespace std;

class Solution
{
public:
    int maxProfit(vector<int> &prices)
    {
        // Cadane Algorithm custom
        int global_value = 0;
        int cur_value = 0;
        int cur_index = -1; // 마지막으로 산 index,
        //cur_index에 해당하는 prices가 증가했다면 무조건 팜 
        //팔면서 cur_value을 증가시킴
        // global을 깨면 ㄱㄱ
        
    }
};